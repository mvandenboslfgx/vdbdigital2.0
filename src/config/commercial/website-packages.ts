import {
  getCatalogItem,
  type CommercialCatalogItem,
} from "@/config/commercial/pricing";

/** Configurable website package tiers */
export type WebsitePackageId = "onepage" | "launch" | "growth" | "custom";

export interface WebsitePackageDefinition {
  id: WebsitePackageId;
  slug: string;
  i18nKey: WebsitePackageId;
  maxPages: number | null;
  catalogSlug: string;
  foundingEligible: boolean;
  quoteOnly: boolean;
  sortOrder: number;
  revisionRounds: number;
}

export const websitePackages: WebsitePackageDefinition[] = [
  {
    id: "onepage",
    slug: "onepage-website",
    i18nKey: "onepage",
    maxPages: 1,
    catalogSlug: "onepage-website",
    foundingEligible: true,
    quoteOnly: false,
    sortOrder: 1,
    revisionRounds: 1,
  },
  {
    id: "launch",
    slug: "launch-website",
    i18nKey: "launch",
    maxPages: 3,
    catalogSlug: "launch-website",
    foundingEligible: true,
    quoteOnly: false,
    sortOrder: 2,
    revisionRounds: 2,
  },
  {
    id: "growth",
    slug: "growth-website",
    i18nKey: "growth",
    maxPages: 7,
    catalogSlug: "growth-website",
    foundingEligible: true,
    quoteOnly: false,
    sortOrder: 3,
    revisionRounds: 3,
  },
  {
    id: "custom",
    slug: "custom-website",
    i18nKey: "custom",
    maxPages: null,
    catalogSlug: "custom-website",
    foundingEligible: false,
    quoteOnly: true,
    sortOrder: 4,
    revisionRounds: 0,
  },
];

export function getPackageCatalogItem(
  pkg: WebsitePackageDefinition,
): CommercialCatalogItem | undefined {
  return getCatalogItem(pkg.catalogSlug);
}
