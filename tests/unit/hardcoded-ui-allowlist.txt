# Known hardcoded-UI-string debt, allowlisted by file (not by line).
# Format: one repo-relative path per line. `#` starts a comment.
# Read by tests/unit/hardcoded-ui-string-scan.test.ts — do not add new
# entries casually; prefer fixing the hardcoded copy via the i18n dictionary.
#
# Auth flow pages predate the next-intl adoption (0ac3d7a) and are still
# fully Dutch-hardcoded; not yet migrated to the dictionary.
src/app/(auth)/geen-toegang/page.tsx
src/app/(auth)/inloggen/page.tsx
src/app/(auth)/wachtwoord-vergeten/page.tsx
src/components/auth/auth-login-form.tsx
# Customer-facing but still hardcoded Dutch copy — flagged for Phase 7 i18n cleanup.
src/app/(shop)/cart/page.tsx
#
# =============================================================================
# Admin surfaces
# =============================================================================
# Admin is IN SCOPE for this scanner and the release bar is EN+NL with English
# fallback — Dutch-only admin chrome is a defect, not an accepted design. The
# entries below are therefore *tracked debt with a deadline*, not permanent
# exceptions. Everything reachable from the sidebar (dashboard, all list
# pages, the settings/roles/users/audit pages, the product translation
# workflow and the product preview) is already fully localized and must stay
# off this list.
#
# --- Admin record detail pages -----------------------------------------------
# Second-level pages reached from a list row. Each mixes localized enum labels
# (already routed through labelFor()) with a long tail of page-specific Dutch
# headings, activity-timeline copy and empty states. Deferred as one batch so
# the copy is reviewed against the record schemas in a single pass rather than
# guessed page by page.
src/app/admin/(protected)/customers/[id]/page.tsx
src/app/admin/(protected)/documents/[id]/page.tsx
src/app/admin/(protected)/invoices/[id]/page.tsx
src/app/admin/(protected)/invoices/[id]/preview/page.tsx
src/app/admin/(protected)/invoices/[id]/versions/page.tsx
src/app/admin/(protected)/quotes/[id]/page.tsx
src/app/admin/(protected)/quotes/[id]/versions/page.tsx
src/app/admin/(protected)/support/[id]/page.tsx
src/app/admin/(protected)/projects/[id]/actions/page.tsx
src/app/admin/(protected)/projects/[id]/activity/page.tsx
src/app/admin/(protected)/projects/[id]/deliverables/page.tsx
src/app/admin/(protected)/projects/[id]/documents/page.tsx
src/app/admin/(protected)/projects/[id]/feedback/page.tsx
src/app/admin/(protected)/projects/[id]/milestones/page.tsx
src/app/admin/(protected)/projects/[id]/overview/page.tsx
#
# --- Admin editor / manager client components --------------------------------
# Large client components whose copy has to be resolved server-side and passed
# in as props (they cannot call getDictionary()). The product editor's
# multilingual section was extracted into the fully localized
# TranslationWorkflowPanel; its remaining sections (pricing, partner terms,
# legal approval) and the components below still carry Dutch labels and need
# the same labels-prop treatment.
src/components/admin/admin-products-table.tsx
src/components/admin/admin-ticket-internal-note-form.tsx
src/components/admin/admin-ticket-status-form.tsx
src/components/admin/create-customer-form.tsx
src/components/admin/invoice-editor-form.tsx
src/components/admin/product-editor-form.tsx
src/components/admin/project-forms.tsx
src/components/admin/project-settings-form.tsx
src/components/admin/quote-editor-form.tsx
src/components/admin/reverse-payment-controls.tsx
